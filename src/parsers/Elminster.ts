import Enchiridion from '../../main';
import {TreeNode} from './Marcus';

export default class Elminster {
	plugin: Enchiridion;

	constructor(plugin: Enchiridion) {
		this.plugin = plugin;
	}

	parse(tree: TreeNode) {

	}

	findKey(key: string, tree: TreeNode) {

	}
}
